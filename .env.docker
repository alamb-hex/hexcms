# heXcms Docker Compose Environment
#
# This file contains environment variables for running heXcms with Docker Compose.
# The Docker Compose stack includes PostgreSQL 16.

# ============================================================================
# Database Driver
# ============================================================================

# Use native PostgreSQL driver for Docker deployment
DATABASE_DRIVER=postgres

# ============================================================================
# Database (Docker PostgreSQL)
# ============================================================================

# PostgreSQL connection string (matches docker-compose.yml service)
DATABASE_URL=postgres://hexcms:hexcms@postgres:5432/hexcms

# Connection pool settings (optional)
DATABASE_POOL_MIN=2
DATABASE_POOL_MAX=10

# ============================================================================
# GitHub Integration (Required for content sync)
# ============================================================================

# GitHub personal access token with repo permissions
# Create at: https://github.com/settings/tokens/new
# Scopes needed: repo (full control)
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Your content repository details
GITHUB_REPO_OWNER=your-username
GITHUB_REPO_NAME=hexcms-content

# GitHub webhook secret (generate a random string)
# Generate with: openssl rand -hex 32
GITHUB_WEBHOOK_SECRET=your-webhook-secret-here

# ============================================================================
# Application
# ============================================================================

# Site URL
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Node environment
NODE_ENV=development

# ============================================================================
# Revalidation (Optional)
# ============================================================================

# Secret for on-demand revalidation API endpoint
# Generate with: openssl rand -hex 32
REVALIDATION_SECRET=your-revalidation-secret-here
